# Airflow Provider Packages for Astronomical Data Pipeline
# Version: 2024-10-10
# Compatible with apache/airflow:2.7.1 (Python 3.8)

# AWS Integration (S3, Lambda, IAM)
apache-airflow-providers-amazon==8.10.0

# Kubernetes Integration (EKS, KubernetesPodOperator)
apache-airflow-providers-cncf-kubernetes==7.9.0

# PostgreSQL Integration (Database operators)
apache-airflow-providers-postgres==5.7.1

# HTTP Integration (REST API calls)
apache-airflow-providers-http==4.6.0

# Additional useful providers
apache-airflow-providers-common-sql==1.8.1

# Python dependencies for DAGs
boto3==1.34.16
requests==2.31.0
astropy==5.2.2  # For FITS file handling in validation tasks (compatible with Python 3.8)
